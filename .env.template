# ClickUp connection
CLICKUP_API_TOKEN=
CLICKUP_LIST_ID=
CLICKUP_SF_ID_FIELD_ID=

# HMAC secret for client URL signatures
CLIENT_LINK_SECRET=

# Optional admin API key for /admin/* endpoints.
# If empty, admin endpoints are open on your local environment.
ADMIN_API_KEY=

# Optional webhook token. If set, include it in ClickUp webhook URL query string:
# https://your-domain/webhook/clickup?token=<WEBHOOK_TOKEN>
WEBHOOK_TOKEN=

# Field map for dashboard layout: label_key -> ClickUp custom field ID
# Key patterns:
# - project.<field>                  (top Project Details section)
# - project.sra_enabled              (checkbox: show/hide SRA section)
# - project.nva_enabled              (checkbox: show/hide NVA section)
# - sra.<step_name>.<field>          (SRA Track step cards)
# - nva.<step_name>.<field>          (NVA Track step cards)
# - anything else                    (Additional Metrics section)
# Example:
# {
#   "project.sra_enabled":"cf_sra_enabled_checkbox",
#   "project.nva_enabled":"cf_nva_enabled_checkbox",
#   "project.ecd":"cf_project_ecd",
#   "project.project_lead":"cf_project_lead",
#   "sra.sra_kickoff.status":"cf_sra_kickoff_status",
#   "sra.sra_kickoff.owner":"cf_sra_kickoff_owner",
#   "nva.verify_access.status":"cf_nva_verify_access_status",
#   "nva.verify_access.owner":"cf_nva_verify_access_owner"
# }
CLICKUP_FIELD_MAP_JSON={}

DATABASE_PATH=client_status.db
PORT=8080
